package org.projects.market.domain;

public enum USER_ROLE {              //enum is used to allow only the specific user role or else throw exception.
    ROLE_ADMIN,
    ROLE_COSTUMER,
    ROLE_SELLER
}
